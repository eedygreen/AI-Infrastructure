name: training_validation
conda_env: conda.yml

entry_points:
  main:
    parameters:
      hydra_options:
        description: Hydra parameters to override
        type: str
        defaults: ""
    command: >-
      python main.py $(echo {hydra_options})
        